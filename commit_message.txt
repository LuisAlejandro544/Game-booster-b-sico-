feat: automated gamer DND mode, app whitelist exceptions, and live HUD hibernation manager

- Add Gamer DND Mode (`GamerDndController`) via Shizuku shell control for `zen_mode` and `heads_up_notifications_enabled`
- Support custom app whitelist exceptions and priority incoming phone calls pass-through
- Add custom background app hibernation targets and wake exceptions manager in `BoosterPreferences` and `ProcessHibernationController`
- Integrate DND and Hibernation management tabs into the floating in-game HUD (`HudDndTab`, `HudHibernationTab`) with real-time feedback
- Update `GameConfigSheet`, `GameWatcherService`, and `GameBoostOrchestrator` to configure and automatically activate DND and hibernation on game launch
- Guarantee full automated state restoration (DND, heads-up, suspended apps, GMS) upon game exit or process termination via `GameWatcherService` and `BootRecoveryReceiver`
- Update all documentation files (README.md, ROADMAP.md, STRUCTURE.md, AI_CONTEXT.md)
