fix(hud): add vertical scroll to overlay, redesign minimize button, and allow minimizing during resolution tests

- Fix cramped UI layout on landscape/compact screens by adding `verticalScroll` to the HUD panel content and applying responsive height constraints
- Redesign window minimize button with an intuitive cyan minus icon (`Icons.Default.Remove`) in a dedicated circular surface, clearly distinguishing it from the close button
- Enable seamless HUD minimization during active 15-second resolution test mode without killing or disrupting the timer/service
- Add live glowing amber countdown badge (`⏱️ Xs`) directly on `FloatingGamerBubble` when minimized during resolution testing
- Prevent awkward tab title text wrapping (e.g., "Resoluc\nión") by refining tab layout with single-line text and tuned typography
- Auto-minimize HUD panel to floating bubble upon directly applying or confirming a display resolution scale for seamless return to gameplay
