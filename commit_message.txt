feat(display): add 5-layer failsafe resolution and proportional DPI scaling per game

- Implement DisplayScaleController managing elevated wm size and wm density adjustments via Shizuku
- Add 5-layer safety failsafe architecture:
  1. Watchdog Daemon (Dead-Man's Switch): Detached shell script in /data/local/tmp with 35s timeout restoring factory display if app terminates
  2. Persistent Panic Button: High-priority notification with immediate broadcast receiver (EmergencyResetReceiver)
  3. Boot Recovery: Automatic rollback check in BootRecoveryReceiver if phone reboots
  4. Safe Proportional Clamping: Even-pixel resolution rounding and proportional DPI to prevent touch desynchronization
  5. Live 15-Second Test Mode: Interactive in-app countdown with auto-rollback if user does not confirm
- Add resolution scaling selector (Native 100%, Balanced 85%, Performance 75%, Ultra Smooth 50%) to GameConfigSheet
- Persist display scale profiles per game in BoosterPreferences and GameItem
- Integrate display scale lifecycle hooks into GameWatcherService, GameBoostOrchestrator and BoosterViewModel

fix(overlay): enable smooth free-dragging on HUD bubble & remove pulse scaling animation

- Implement responsive touch drag gestures via Compose awaitEachGesture pointer input
- Connect HUD drag delta to WindowManager layout params in DraggableOverlayWindowManager
- Add header dragging support for the expanded gaming panel
- Remove infinite pulse/scaling animation from FloatingGamerBubble for a clean and stable chip design
- Ensure overlay position stays within device screen bounds

feat(catalog): require manual addition of games to gamer library with search & filter chips

- Stop auto-populating all installed games and apps into the Gamer Library
- Require users to explicitly search and add their chosen games and apps via the "+ Añadir App" sheet
- Add filter chips ("Todos", "Solo Juegos", "Otras Apps") in AppPickerSheet for fast game discovery
- Display "AÑADIDO" visual badge and checkmark for applications already present in the user's library
- Keep gamer library clean and personalized to user preferences

feat(telemetry): add zero-allocation native C++ metrics and high-precision frametime stats

- Implement zero-allocation POSIX open/read polling in native-lib.cpp for /proc/stat and kernel thermal zones to prevent Garbage Collector jank
- Add high-precision CLOCK_MONOTONIC_RAW nanosecond frametime tracking in a 120-frame native ring buffer
- Calculate real-time average frametime latency (ms/frame) and 1% Low / 0.1% Low FPS percentiles
- Expose native frametime stats via NativeEngineBridge and connect to DeviceMetrics
- Add zero-allocation frametime and 1% Low FPS cards into in-game HudTelemetryTab
- Update documentation files (AI_CONTEXT.md, ROADMAP.md, STRUCTURE.md) to reflect native engine capabilities

