feat(shizuku): implement OOM score protection and special-use foreground service immunity

- Blindaje de proceso y protección OOM Score (-1000) mediante Shizuku para evitar que Android LMK mate la app en juegos pesados
- Inclusión automática en la lista blanca de Doze (`dumpsys deviceidle whitelist +pkg`) y concesión de AppOps en segundo plano
- Integración estricta de `FOREGROUND_SERVICE_TYPE_SPECIAL_USE` en `GameWatcherService` y `GameOverlayService` para Android 14/15
- Limpieza y restauración segura de políticas de proceso al salir de la partida y en `BootRecoveryReceiver`
